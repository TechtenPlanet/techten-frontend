{
  "name": "techten-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Techten Planet frontend and Hapi Notion Proxy backend",
  "main": "index.js",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf backend/public && mkdir -p backend/public && cp -R frontend/build/* backend/public/",
    "start": "npm --prefix backend start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
